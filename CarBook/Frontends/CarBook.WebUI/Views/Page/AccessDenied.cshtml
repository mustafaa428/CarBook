
@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yetkisiz Giriş</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #a8d8ff, #d9f1ff); /* Daha açık mavi tonları */
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #333;
        }

        .card {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 40px;
            border-radius: 15px;
            text-align: center;
            width: 350px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
        }

            .card h1 {
                font-size: 2em;
                margin-bottom: 15px;
                color: #007acc;
            }

            .card p {
                font-size: 1.1em;
                margin-bottom: 25px;
                color: #555;
            }

        .btn {
            display: inline-block;
            padding: 12px 24px;
            background-color: #007acc;
            color: #fff;
            font-weight: bold;
            text-decoration: none;
            border-radius: 8px;
            transition: 0.3s;
        }

            .btn:hover {
                background-color: #005fa3;
                transform: scale(1.05);
            }
    </style>
</head>
<body>
    <div class="card">
        <h1>Yetkisiz Giriş</h1>
        <p>Bu sayfaya erişim izniniz bulunmamaktadır.</p>
        <a href="/Login/Index" class="btn">Ana Sayfaya Dön</a>
    </div>
</body>
</html>
